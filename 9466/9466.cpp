#include <iostream>
#include <queue>

int main(void)
{
	std::cin.tie(NULL);
	std::ios::sync_with_stdio(false);

	int T;
	std::cin >> T;

	for (int loop = 0; loop < T; ++loop)
	{
		int choice[100001] = {0, };
		int N;
		std::cin >> N;
		for (int i = 1; i <= N; ++i)
		{
			std::cin >> choice[i];
		}
		int count = 0;
		for (int i = 1; i <= N; ++i)
		{
			int member = 1;
			
		}
	}

	return 0;
}
